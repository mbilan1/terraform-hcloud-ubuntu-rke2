# ── Hetzner Cloud ─────────────────────────────────────────────
hcloud_token = "YOUR_HETZNER_TOKEN"

# ── AWS / Route53 DNS ─────────────────────────────────────────
aws_access_key = "AKIAIOSFODNN7EXAMPLE"
aws_secret_key = "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
aws_region     = "eu-central-1"

# ── DNS ───────────────────────────────────────────────────────
route53_zone_name   = "example.com"
route53_record_name = "openedx"           # → openedx.example.com

# NOTE: Keep these aligned to avoid confusing DNS/TLS behavior:
# domain == "${route53_record_name}.${route53_zone_name}"
domain = "openedx.example.com"

# ── Optional ──────────────────────────────────────────────────
cluster_name      = "openedx"
letsencrypt_email = "admin@example.com"

# ── Backup (Hetzner Object Storage) ──────────────────────────
# enable_backups       = true
# backup_s3_bucket     = "my-openedx-backups"
# backup_s3_access_key = "YOUR_S3_ACCESS_KEY"
# backup_s3_secret_key = "YOUR_S3_SECRET_KEY"
